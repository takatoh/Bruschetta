services:

  bruschetta:
    image: "bruschetta-api:${TAG-latest}"
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 5000:5000
    volumes:
      - .:/app:cached
    tty: true
    restart: unless-stopped
